services:
  - type: web
    name: vampiro-multiplayer
    runtime: python
    plan: free
    rootDir: multiplayer_server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
